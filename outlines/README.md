# Outlines

Runbook A writes each Markdown layout outline here as `<page-slug>.md`.

An outline in this folder is a draft until a human fills its `Approved-By:`
and `Approval-Date:` lines and flips `Ready for Runbook B:` to `Yes`. Runbook
B reads the approved outline from this folder and refuses to start without
that approval record.
