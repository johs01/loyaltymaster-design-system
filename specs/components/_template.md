# Component Name

Component ID: `component-id`
Registry: `registry/components.json`
Status: `raw-snapshot`

## Purpose

State the component's job in one or two sentences.

## When To Use

- Use this component when the page has the exact need recorded in the registry.

## When Not To Use

- Do not use this component as a generic replacement for unrelated patterns.

## Props

List the approved props and required fields. Do not add props without updating
`registry/components.json` and the future library implementation.

## States

List default, hover, focus, active, disabled, loading, empty, error, and
responsive states that apply. Mark non-applicable states as not applicable.

## Accessibility Rules

- Preserve semantic HTML.
- Provide visible focus for every interactive element.
- Use real labels for inputs and controls.
- Respect reduced-motion preferences where motion exists.

## Screenshot

Reference the approved screenshot from `assets/screenshots/`.

## Token Usage

List every approved token used by this component. Do not replace tokens with raw
literal values.

## Composition Rules

Explain where this component may sit in a page and which components it may or
may not be paired with.

## Design Rules

Document the visual rules that keep this component on brand.

## AI Usage Contract

- Read this spec immediately before using the component.
- Use the current version of this spec if rules have changed.
- Do not import from `/Components/`.
- Do not invent visual variants, colors, radii, shadows, type styles, or
  compositions without approval.

