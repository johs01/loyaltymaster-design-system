# Strategy Sentence Cards — Evidence

Handoff evidence for the canon-promoted `strategy-sentence-cards` registry
component (promoted from the approved 2026-07-10 loyaltymaster.com site
rollout).

- `Strategy Sentence Cards.tsx` — frozen snapshot of the production
  `FootTrafficOnDemandPage.tsx` source where the ftd-points pattern
  originated (copied from the read-only sendPUSH replica; evidence only,
  never a runtime import source).
- `strategy-sentence-cards.png` — the approved visual reference (same image
  as `assets/screenshots/strategy-sentence-cards.png`).

Runtime implementation: `library/src/components/StrategySentenceCards.tsx`
via the registry `libraryPath`.
