# Contact Form Section

Approved MagicPath visual handoff archive for the `Contact Form Section` approved component.

## MagicPath Metadata

- Component name: `Contact Form Section`
- Generated name: `bold-moon-9702`
- Component ID: `409569352514408448`
- Project: `SendPUSH ALL SECTIONS - PROD`
- Project ID: `382091404937150464`
- Preview URL: https://api.magicpath.ai/v1/bold-moon-9702
- MagicPath updated at: `2026-05-25T10:13:32.998Z`
- MagicPath revision count at export: `21`
- Export date: 2026-05-25T10:42:10.170Z

## Exported Files

- `ContactFormSection.tsx` - archive alias that exports the approved component name
- `AwardsStrip.tsx` - MagicPath exported source
- `ContactUsPage.tsx` - MagicPath exported source
- `TestimonialSection.tsx` - MagicPath exported source

## Dependencies

- `class-variance-authority`
- `clsx`
- `framer-motion`
- `lucide-react`
- `tailwind-merge`

## MagicPath Reported Import

```tsx
import { AwardsStrip } from '@/components/magicpath/contact-form-section/AwardsStrip';
```

## MagicPath Reported Usage

```tsx
<AwardsStrip />
```

## Archive Import

```tsx
import { ContactFormSection } from './ContactFormSection';
```

## Archive Usage

```tsx
<ContactFormSection />
```

## Handoff Note

This folder is an approved visual handoff archive from MagicPath. It is not a design-system registry implementation and does not imply registry approval.

MagicPath exported the rendered visual as `ContactUsPage` plus supporting files. `ContactFormSection.tsx` is a handoff alias so this archive also exposes the approved component name without changing the exported visual source.

This archive was refreshed after the approved mobile view was edited in MagicPath.
